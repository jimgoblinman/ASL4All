// Loading.js

import React from 'react';

const Loading = () => {
  return (
    <div className="loading">
      <h2>Loading...</h2>
      {/* You can add a spinning loader animation or any other visual indication here */}
    </div>
  );
};

export default Loading;